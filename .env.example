# Database
DB_NAME=pinote
DB_USER=pinote
DB_PASS=pinote

# Ollama (Galaxy Note 10 IP - update this!)
OLLAMA_HOST=192.168.219.201
OLLAMA_PORT=11434
OLLAMA_MODEL=qwen2.5:0.5b
OLLAMA_TIMEOUT=300

# CORS
CORS_ORIGINS=http://localhost:3080,http://localhost

# JVM (Mac default)
JAVA_OPTS=-Xms128m -Xmx512m

# Frontend port (Mac: 3080, Pi: 80)
FRONTEND_PORT=3080

# --- Pi (ARM32) overrides ---
# Create .env on Pi with these values:
# POSTGRES_IMAGE=arm32v7/postgres:15-alpine
# BACKEND_RUNTIME=arm32v7/eclipse-temurin:21-jre
# FRONTEND_RUNTIME=arm32v7/nginx:1.25-alpine
# FRONTEND_PORT=80
# CORS_ORIGINS=http://localhost
# JAVA_OPTS=-Xms64m -Xmx192m -XX:+UseSerialGC -XX:MaxMetaspaceSize=64m -Xss512k -XX:TieredStopAtLevel=1 -XX:ReservedCodeCacheSize=32m
